function [H, C] = hists(hist)
%HISTS Summary of this function goes here
%   Detailed explanation goes here
[H,x] = imhist(I);
tmpC = cumsum(H);
k = max(tmpC)/max(H);
C = tmpC/k;
figure;
subplot(1,2,1);
imshow(I);
subplot(1,2,2);
plot(x,H);
hold on;
plot(x,C2);


end

